# Quote card company-name truncation

- Quote selector cards use a fixed 300 px carousel width so long client names cannot expand the card.
- The logo/name section is allowed to shrink, while the quote-status badge keeps its natural width.
- Company names remain on one line and use an ellipsis when space runs out.
- The full company name remains available as the native hover title.
