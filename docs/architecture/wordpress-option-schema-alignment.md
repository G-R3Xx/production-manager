# WordPress option schema alignment

Version: V26.07.29.01

Production Manager remains the source of truth for customer choices. The WordPress catalogue now receives the exact field order, labels, defaults, required state, option values, price adjustments and custom-size behaviour used by Production Manager. Standard internal fields are normalised before publishing, while advanced fields retain their saved order.
