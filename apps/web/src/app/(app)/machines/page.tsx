import { redirect } from "next/navigation";
import { getRequiredSessionUser } from "@/server/auth/session";
import { resolveActiveTenantForAuthUserId } from "@/server/bootstrap/activeTenant";
import { listMachinesForTenant } from "@/server/productionResources";
import { createMachineAction,setMachineActiveAction } from "./actions";
const card={background:"#fff",border:"1px solid #dbe4f0",borderRadius:20,padding:20} as const; const input={minHeight:42,border:"1px solid #cfd8e3",borderRadius:10,padding:"0 11px",width:"100%",boxSizing:"border-box"} as const;
export default async function MachinesPage(){const u=await getRequiredSessionUser();const t=await resolveActiveTenantForAuthUserId(u.id);if(!t)redirect("/bootstrap");const rows=await listMachinesForTenant(t.tenantId);return <main style={{display:"grid",gap:20}}><header><div style={{fontSize:12,fontWeight:900,color:"#0284c7",textTransform:"uppercase"}}>Production resources</div><h1 style={{margin:"6px 0",fontSize:36}}>Machines</h1><p style={{color:"#64748b"}}>Printers, cutters and finishing equipment with real speed and operating costs.</p></header><section style={card}><h2 style={{marginTop:0}}>Add machine</h2><form action={createMachineAction} style={{display:"grid",gridTemplateColumns:"2fr 1fr repeat(6,1fr) auto",gap:10,alignItems:"end"}}><label>Name<input name="name" style={input}/></label><label>Type<select name="machineType" style={input}><option value="printer">Printer</option><option value="cutter">Cutter</option><option value="laminator">Laminator</option><option value="other">Other</option></select></label><label>Max width<input name="maxWidthMm" type="number" style={input}/></label><label>Speed<input name="speedValue" type="number" step="0.01" style={input}/></label><label>Speed unit<select name="speedUom" style={input}><option value="sqm_per_hour">m²/hr</option><option value="linear_metres_per_hour">lm/hr</option><option value="sheets_per_hour">sheets/hr</option></select></label><label>$/hr<input name="hourlyCost" type="number" step="0.01" style={input}/></label><label>Setup min<input name="setupMinutes" type="number" step="0.1" style={input}/></label><label>Ink $/m²<input name="inkCostPerSqm" type="number" step="0.01" style={input}/></label><button style={{minHeight:42,border:0,borderRadius:10,background:"#2563eb",color:"#fff",fontWeight:900,padding:"0 16px"}}>Add</button></form></section><section style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(300px,1fr))",gap:14}}>{rows.map(r=><article key={r.id} style={{...card,opacity:r.active?1:.6}}><div style={{display:"flex",justifyContent:"space-between",gap:12}}><div><h3 style={{margin:"0 0 6px"}}>{r.name}</h3><div style={{color:"#64748b"}}>{r.machineType} · {r.maxWidthMm||"—"} mm max</div></div><form action={setMachineActiveAction}><input type="hidden" name="id" value={r.id}/><input type="hidden" name="active" value={r.active?"false":"true"}/><button style={{border:"1px solid #cbd5e1",borderRadius:9,background:"#fff",padding:"8px 10px"}}>{r.active?"Archive":"Restore"}</button></form></div><div style={{marginTop:16,display:"grid",gridTemplateColumns:"repeat(2,1fr)",gap:8,fontSize:14}}><span>Speed <b>{r.speedValue} {r.speedUom}</b></span><span>Cost <b>${r.hourlyCost}/hr</b></span><span>Setup <b>{r.setupMinutes} min</b></span><span>Ink <b>${r.inkCostPerSqm}/m²</b></span></div></article>)}</section></main>}
