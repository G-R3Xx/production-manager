# Production Manager V26.08.19.06

- Makes Mark all read operate instantly inside the Alerts popover.
- Removes the app-wide layout revalidation and page reload from this action.
- Updates the unread badge and alert styling optimistically while saving in the background.
- Restores the previous alert state and displays an inline message if persistence fails.
