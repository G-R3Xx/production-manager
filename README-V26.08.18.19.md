# Production Manager V26.08.18.19

- Makes the Job workspace primary button describe the actual next workflow process.
- Shows a large prominent `Create Quote` action while a Job is at Quote Required.
- Uses an existing draft when available without changing the action label to `Open quote`.
- Removes the duplicate secondary Quote button while quoting is the current stage.
- Aligns the displayed next action with `Create quote`.
