# Production Manager V26.07.29.06

## WordPress live option pricing and dependencies

- Calculates answer-linked Guided Product Builder components during WordPress live pricing.
- Selected laminate materials now add their real material usage, waste, markup and profit to the configured price.
- Roll print media, ink charges and eyelet material/labour rows use the same answer-linked pricing path.
- Mirrors Guided Builder costing components onto the product website configuration as a fallback alongside the field schema.
- Normalises older Roll stock dependencies so roll-media questions only appear when `Roll print / applied media` is selected.
- Keeps the normal internal Product and Quick Quote workflows unchanged.
- Updates the visible application and WordPress catalogue version to `V26.07.29.06`.
- No database migration is required.
