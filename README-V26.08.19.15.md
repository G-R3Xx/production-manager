# Production Manager V26.08.19.15

- Promotes the Artwork workspace email action into the clear primary client-send control.
- Adds a prominent blue gradient, envelope icon, recipient address, directional cue and hover feedback.
- Shows `Sending artwork approval…` and disables the button while the email is being processed.
- Renames the manual fallback to `Mark sent without email` and places it under secondary actions.
- Refreshes the Artwork workspace immediately after sending so the lifecycle status updates reliably.
- Adds lightweight live Artwork status checks every five seconds and refreshes only when the selected approval actually changes.
- Client views, approvals and change requests now appear without a manual page refresh while avoiding interruptions during form editing.
