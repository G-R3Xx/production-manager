# Production Manager V26.08.18.20

- Creates surveyed signs as blank quote configuration shells containing only survey reference details and measured size.
- Shows every required signage component from the start: sign type, substrate, colour, size, artwork, print, finishing, dispatch and quantity/notes.
- Allows each component to be completed and saved progressively without requiring all other fields first.
- Removes the fake `Survey item — configure material / print` component from new and existing survey lines.
- Keeps a survey line marked as needing configuration until all required pricing choices are complete.
- Ignores generated TypeScript build-cache files.
