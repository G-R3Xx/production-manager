# Production Manager V26.08.18.10

## Job-centred operations foundation on the current baseline

- Adds the active-job dashboard, canonical Job workspace, staff tasks and monthly calendar.
- Derives each job's current stage from the existing enquiry, survey, quote, artwork, production and dispatch records.
- Carries completed production jobs into Invoice required until they are marked invoiced.
- Preserves the complete V26.08.18.09 website discount, artwork proof, job-sheet, MYOB, live-refresh and production functionality.
- Keeps the `app-pulse` endpoint and notification activity export aligned so the web package typechecks cleanly.

This build is based directly on V26.08.18.09 and does not remove any files or functionality from that baseline.
