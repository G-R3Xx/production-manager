# Production Manager V26.07.29.07

## WordPress storefront price and option cleanup

- Publishes the default Guided Builder configuration as the WooCommerce starting price so the page price matches the live Production Manager result.
- Removes empty customer-choice groups such as Finishing when no options were enabled.
- Normalises size labels to a clean format such as `600 × 900 mm`.
- Keeps answer-linked laminate, print, ink, finishing and fulfilment pricing unchanged.
- Updates the visible application and WordPress catalogue version to `V26.07.29.07`.
- No database migration is required.
